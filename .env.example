# GitHub API token to avoid rate limits (recommended)
# Fine-grained token typically starts with: github_pat_
# Classic token typically starts with: ghp_

GITHUB_TOKEN=

# Most hosts set PORT automatically; local default is fine
PORT=3000

# Set to production on the host
NODE_ENV=production